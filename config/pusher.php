<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Pusher config
    |--------------------------------------------------------------------------
    |
    */

    'id' => env('PUSHER_ID'),
    'secret' => env('PUSHER_SECRET'),
    'cluster' => env('PUSHER_CLUSTER'),

];
